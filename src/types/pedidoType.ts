export type Pedido = {
    id: number,
    link: string,
    foto: string,
    titulo: string,
    preco: number
}

export type RegistroPedido = {
    link: string,
    foto: string,
    titulo: string,
    preco: number
}