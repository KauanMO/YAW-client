export type Pedido = {
    id: number,
    link: string,
    foto: string,
    titulo: string,
    preco: number,
    descricao: string
}

export type RegistroPedido = {
    link: string,
    foto: string,
    titulo: string,
    preco: number,
    descricao: string
}